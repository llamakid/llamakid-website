import { llamakidConsoleBanner } from './llamakidBanner'

export function logConsoleArt() {
  llamakidConsoleBanner('Looking under the hood? Building something, hiring, or just curious — say hi: itsnateguy@gmail.com')
}
